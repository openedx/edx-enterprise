coverage:
  status:
    project:
      default:
        enabled: yes
        target: auto
    patch:
      default:
        enabled: yes
        target: 100%

comment: false
ignore:
  - "enterprise/management/commands/produce_event_bus_group_delete_test_event.py"
